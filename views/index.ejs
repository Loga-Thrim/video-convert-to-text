<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="preconnect" href="https://fonts.gstatic.com">
    <link href="https://fonts.googleapis.com/css2?family=Sarabun:wght@300&display=swap" rel="stylesheet">
    <title>convert to mp3</title>

    <style>
        .container{
            width: 100%;
            height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
            font-family: 'Sarabun', sans-serif;
        }
        form{
            width: 50%;
            padding-bottom: 30px;
            background: rgb(255, 0, 55);
            text-align: center;
            color: white;
        }
        .boxinput{
            width: 100%;
            display: flex;
            flex-direction: row;
            justify-content: space-around;
            align-items: center;
        }
        label{
            font-size: 16px;
            background: rgb(235, 68, 104);
            padding: 10px;
            border: 0;
            border-radius: 5px;
            cursor: pointer;
            box-shadow: 1px 1px 2px rgb(240, 240, 240);
        }
        label:hover{
            background: rgb(209, 9, 52);
            box-shadow: 1px 1px 2px rgb(100, 100, 100);
        }
        .fileinput{
            padding: 10px;
            color: white;
            background:rgb(0, 199, 90);
            cursor: pointer;
            border: 0;
            border-radius: 5px;
            font-size: 16px;
            box-shadow: 1px 1px 2px rgb(240, 240, 240);
        }
        .fileinput:hover{
            background:rgb(3, 141, 65);
            box-shadow: 1px 1px 2px rgb(100, 100, 100);
        }
        .nameinput{
            padding: 10px;
            outline: none;
            border: 1px solid rgb(230, 230, 230);
            border-radius: 5px;
            font-size: 16px;
        }

        .bgLoader{
            width: 100%;
            height: 100vh;
            position: fixed;
            background: rgba(0, 0, 0, .9);
            display: flex;
            align-items: center;
            justify-content: center;
            display: none;
        }
        .loader {
            border: 16px solid #f3f3f3;
            border-radius: 50%;
            border-top: 16px solid #3498db;
            width: 150px;
            height: 150px;
            animation: spin 2s linear infinite;
            position: absolute;
        }
        a{
            color: white;
            font-size: 16px;
        }
        @keyframes spin {
            0% { transform: rotate(0deg); }
            100% { transform: rotate(360deg); }
        }
    </style>
</head>
<body style="margin: 0; padding: 0;">
    <div class="container">
        <form method="post" action="/convert" enctype="multipart/form-data">
            <h1>Convert video to text</h2>
            
            <div class="boxinput">
                <input type="text" placeholder="ระบุชื่อวีดีโอ .." name="videoName" class="nameinput" >
                <i class="fas fa-arrow-right"></i>
                <label for="file">Choose video file</label>
                <input type="file" name="mp4" id="file" style="display: none;" class="fileinput">
                <i class="fas fa-arrow-right"></i>
                <input onclick="convertProgress()" type="submit" class="fileinput" value="Convert to text">
            </div> <br><br>
            <a href="/history">ประวัติการใช้งาน</a>
        </form>
        
        <div class="bgLoader"  id="converting">
            <div class="loader"></div>
        </div>
    </div>

    <script>
        function convertProgress(){
            document.getElementById("converting").style.display = "flex";
        }
    </script>
    <script src="https://kit.fontawesome.com/c5723b6e8a.js" crossorigin="anonymous"></script>
</body>
</html>